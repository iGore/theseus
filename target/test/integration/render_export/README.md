# F-006 Integration Scenarios

- renderer golden parity by mode precedence
- nested `--out` path creation and write parity
- stdout emission when `--out` is unset
- `--files` export enabled and omitted behavior
- combined `--out` + `--files` execution path
